<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="us.fecytalk.trycambaru">

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />

    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".StartActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".InputActivity" />
        <activity android:name=".DataShowActivity" />
        <activity android:name=".CameraActivity" />
        <activity android:name=".CamLoginActivity" />
        <activity android:name=".SignInActivity" />
        <activity android:name=".TrainActivity" />
        <activity android:name=".LogInActivity" />
        <activity android:name=".PredictSignature" />
        <activity android:name=".SignatureTrain" />
        <activity android:name=".SendSignature" />
        <activity android:name=".SignatureSignin" />
        <activity android:name=".BeforeQuitActivity"></activity>
    </application>

</manifest>